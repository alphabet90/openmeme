# TEMPLATE ONLY — do not commit real values.
# Populate via Rancher Secrets UI, Sealed Secrets, or External Secrets Operator.
# All values must be base64-encoded.
#
# Encode a value:  echo -n 'myvalue' | base64
apiVersion: v1
kind: Secret
metadata:
  name: api-secret
  namespace: openmeme
  labels:
    app.kubernetes.io/name: memes-api
    app.kubernetes.io/part-of: openmeme
type: Opaque
data:
  # echo -n 'memes' | base64
  DB_USER: bWVtZXM=
  # Replace with a strong password
  DB_PASSWORD: ""
  # Empty by default (Redis without auth)
  REDIS_PASSWORD: ""
  # Replace with a secure random key for the admin API
  ADMIN_API_KEY: ""
