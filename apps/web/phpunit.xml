<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="tests/bootstrap.php"
         colors="true"
         failOnWarning="true"
         defaultTestSuite="unit">
    <testsuites>
        <!-- Pure functions; no Meilisearch or SQLite needed. -->
        <testsuite name="unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <!-- Needs a running Meilisearch (docker compose up) and a built
             index (build-index.php then build-search.php). Select this
             suite with the "testsuite" CLI option of phpunit. -->
        <testsuite name="integration">
            <directory>tests/Integration</directory>
        </testsuite>
    </testsuites>
</phpunit>
